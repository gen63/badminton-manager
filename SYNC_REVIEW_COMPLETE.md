# Firebase同期 徹底レビュー完了

## 発見した問題と修正内容

### 🔴 問題6: push開始とpush完了のタイミングギャップ（最重要）

**問題:**
- push開始からpush完了まで時間がかかる（ネットワーク遅延、Transaction処理）
- push完了後にタイムスタンプを記録していたため、その間にpullが来ると古いデータが適用される

**シナリオ:**
```
Time 0ms:   ユーザーがプレイヤーAを休憩
Time 10ms:  push開始（lastPushedTimeはまだ0）
Time 50ms:  pull受信（古いデータ）→ lastPushedTime=0 → ブロックされない → 適用される ❌
Time 200ms: push完了 → lastPushedTime=200
```

**修正:**
- push**開始時**にタイムスタンプを記録
- これにより、push処理中のpullもブロックされる
- push失敗時はタイムスタンプをリセット（pullを受け付ける）

---

### 🔴 問題7: isSyncingFromRemote のタイミング問題

**問題:**
- ストア更新中に `isSyncingFromRemote.current = false` が即座に実行される
- その間に別の更新が発生すると、pushが予約される可能性

**修正:**
- try-finally でラップし、すべてのストア更新が完了してから必ずフラグをリセット

---

### 🔴 問題10: 初回pushのタイミング問題（重大）

**問題:**
- セッション参加者がMainPageに遷移した直後、初回pushが実行される
- その時点でストアは空（まだゲーム状態をロードしていない）
- 空のデータがFirestoreにpushされ、セッション作成者が追加したプレイヤーが消える

**修正:**
- セッション作成者のみ初回pushを実行
- 参加者は初回pushをスキップ（リモートデータをpullして初期化）
- ページリロード時（プレイヤーが既にいる場合）は例外的にpush

---

### ✅ 既存の修正（前回のコミットで対応済み）

1. **タイムスタンプ取得の堅牢化**
   - `getTimestampMillis()` 関数を新規作成
   - `.toMillis()` メソッド対応
   - 取得失敗時のエラー検出

2. **pullデバウンスを削除**
   - 古いデータ保持問題を解消
   - 即座に判定・適用

3. **詳細ログの追加**
   - タイムスタンプの生値とISO形式
   - SKIP理由の詳細

---

## 最終的な同期フロー

### Push側（ローカル → Firestore）

1. ストア変更検知
2. `isSyncingFromRemote` チェック（pullからの更新なら無視）
3. デバウンス（300ms）
4. **push開始時にタイムスタンプを記録** ← 新規追加
5. Firestore Transaction
6. push成功時、ハッシュを記録

### Pull側（Firestore → ローカル）

1. onSnapshot で通知受信
2. タイムスタンプ取得（堅牢版）
3. ハッシュ計算
4. 判定フロー:
   - 自分がpushしたデータ？ → SKIP
   - タイムスタンプが古い？ → SKIP
   - push開始から500ms以内？ → SKIP ← 修正済み（push開始時に記録）
5. try-finally でストア更新
6. タイムスタンプを記録

---

## まだ残っている潜在的な問題

### 🟡 問題8: ハッシュ計算のタイミング（低優先度）

**問題:**
- ハッシュ計算からpush完了まで時間がかかる
- その間にストアが変更されたら、記録されたハッシュは実際のpushデータと一致しない

**影響:**
- 次のpullで「同じハッシュ」判定が失敗する可能性
- しかし、タイムスタンプによる古いデータチェックがあるため、実害は小さい

**対策案:**
- pushデータのクローンを作成し、そのクローンからハッシュを計算
- または、Firestore Transactionの読み取り結果からハッシュを計算

### 🟡 問題9: JSON.stringify比較のオーバーヘッド（低優先度）

**問題:**
- 毎回JSON.stringifyで大きなオブジェクトをシリアライズ
- パフォーマンス問題
- 配列の順序が異なるだけで「変更あり」と判定

**影響:**
- 若干のパフォーマンス低下
- 実用上は問題ないレベル

**対策案:**
- 軽量なdiff関数を実装
- または、変更検知をReact側に任せる（useMemo等）

---

## 検証方法

1. **2つのブラウザで同時テスト**
   - ブラウザA: プレイヤーを休憩
   - PWA B: 反映されるか？何秒後？
   - ログで push開始 → pull受信 → SKIP/APPLY を確認

2. **セッション参加直後のテスト**
   - ブラウザA: セッション作成 → プレイヤー追加
   - ブラウザB: セッション参加
   - プレイヤーが消えないか確認

3. **ネットワーク遅延のシミュレーション**
   - Chrome DevTools → Network → Slow 3G
   - 古いデータが後から来た時の動作確認
   - ログで タイムスタンプ比較 → SKIP を確認

---

## 今後の改善案（時間がある時）

### Phase 3: 楽観的UI更新

**目的:**
- ユーザー操作の即座の反映
- ネットワーク遅延の影響を受けない

**実装:**
1. 自分の操作は即座にローカルストアに反映
2. Firestoreへのpushは非同期（バックグラウンド）
3. pullは「確認」として扱い、自分が変更した部分は上書きしない
4. 競合発生時のみマージロジックを実行

**メリット:**
- 操作のレスポンスが向上
- ネットワーク遅延の影響を受けない
- より堅牢な同期

**デメリット:**
- 実装が複雑
- マージロジックが必要
